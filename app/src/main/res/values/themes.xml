<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MzansiRecipes" parent="Theme.AppCompat.Light.NoActionBar" />

    <!-- Splash Screen Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">

        <item name="windowSplashScreenBackground">@android:color/white</item>


        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_inset</item>

        <item name="postSplashScreenTheme">@style/Theme.MzansiRecipes</item>
    </style>
</resources>